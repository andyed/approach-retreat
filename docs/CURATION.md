# AR Replay Signal Testbed — Curation Rubric

_Generated by `scripts/select_testbed.py`. Pool: top **80** trials from `attentional-foraging/AdSERP/data/trial-scores.csv` after per-participant diversity capping. Final testbed: **23** trials across 5 groups._

## What this is

This is a **signal testbed**, not a showcase. Each trial is a tagged demonstration of the four-class taxonomy inference task: SERP screenshot + cursor + per-AOI labels overlaid. The viewer renders what a cursor-only classifier emits on this cursor stream.

Per-AOI labels are **predictions**, not ground truth. The classifier uses cursor + AOI bboxes only; gaze and pupil are shown for context. If you disagree with a label on a specific trial, that trial belongs in Group F (failure modes) and the inference logic needs work.

## Inference: M5 (primary) + bbox-episode heuristic (secondary)

Two classifiers run on every organic AOI; both are reported in the trial JSON and in the viewer.

### M5 (primary for organic AOIs)

Logistic regression on the M4 nine-feature approach set, **trained against NB22 gaze-derived labels** (1916 deferred / 439 eval-rejected non-click episodes across 47 AdSERP participants). Trained in `attentional-foraging/scripts/m5_cursor_only_taxonomy.py`; coefficients shipped at `scripts/models/m5_final_model.json`.

- **LOSO AUC:** 0.794 (median per-participant 0.794, IQR [0.707, 0.869])
- **Operating threshold:** p* = 0.395 (Youden-J on out-of-fold predictions)
- **Predicted-deferred precision:** 90.2%
- **Features (cursor-only, no gaze, no scroll):** `min_dist`, `mean_dist`, `final_dist`, `retreat_dist`, `dwell_in_proximity_ms`, `mean_approach_velocity`, `max_approach_velocity`, `direction_changes`, `frac_decreasing` — all geometric on cursor `pageY` relative to AOI y-center.
- **Domain:** organic results only (M5's training population). Ad AOIs fall back to the heuristic.
- **Approached-by-M5 definition:** `min_dist < 100px` (cursor came within 100px of the AOI's y-center). More permissive than the heuristic's bbox-containment definition.

### Bbox-episode heuristic (secondary, all AOIs)

Walk the cursor stream against the AOI bbox. An **episode** is an enter→exit traversal where dwell ≥ 100ms (filters drive-by crossings). Label rules:

- `CLICKED` — any episode contains a click event inside the bbox.
- `DEFERRED` — ≥2 kept episodes (cursor literally re-entered the bbox).
- `EVALUATED_REJECTED` — exactly 1 kept episode.
- `NOT_APPROACHED` — 0 kept episodes.

### Final canonical label (per AOI)

1. `CLICKED` if any bbox episode contains a click event (both classifiers agree on this case).
2. For organic AOIs: M5 prediction (DEFERRED if proba ≥ 0.395, else EVALUATED_REJECTED). NOT_APPROACHED if min_dist ≥ 100px.
3. For ad AOIs: bbox-episode heuristic.

### Why surface both

M5 and the heuristic disagree often (~80% of approached organic AOIs in spot checks). The disagreement is meaningful: M5 sees "deferred-like signature" (close approach + dwell + retreat), the heuristic sees "literal re-approach" (cursor physically re-entered the bbox). Both are valid framings of the four-class taxonomy. Group D and the per-AOI badges (`≠ heur:LABEL` chevron) surface the disagreements directly so a reviewer can decide which framing is more defensible per case.

## Regime tag

**`[CURSOR-ONLY]` algorithm running on `[LAB]` data, supervision was `[LAB, NB22 gaze-derived]`.**

Per the [LAB/WILD convention](../CLAUDE.md#role-in-the-cikm-paper-the-lab--wild-bridge), the four-class taxonomy is currently `[LAB]`-only because the canonical version is gaze-derived. M5 is the named cursor-only-bootstrap that earns `[BOTH]` *if* its agreement with the gaze-derived ground truth is high enough; M5's LOSO AUC of 0.794 against NB22 is the published evidence. The replay viewer is the visual inspection tool for that bootstrap — every disagreement between M5 and the heuristic on a specific AOI is a candidate for human adjudication.

## Selection rubric

1. **Pass 1** (`scripts/score_trials.py` in attentional-foraging) scores every trial on cursor-only features:
   ```
   score = 0.20 * normalize(n_mousemove,    50, 400)
         + 0.25 * normalize(distinct_y_bands, 2,   8)
         + 0.25 * normalize(y_band_revisits,  0,   6)
         + 0.15 * normalize(duration_ms,   5000, 25000)
         + 0.15 * has_click
         × (1 - 0.5 * pupil_dropout_rate)
   ```
   `has_click` is 1.0 for every trial in AdSERP (protocol-required) — kept for transparency but doesn't discriminate.

2. **Pass 2** (this script) takes the top **80** with a per-participant cap, extracts organic AOI bboxes, builds replay bundles with M5 + heuristic labels, and assigns each trial to the most diagnostic group it qualifies for (priority order: E → B-DEF → B-REJ → C → D → A).

## Groups

### Group E — Multi-AOI dramas (6 trials)

Trials with 3+ active label classes (CLICKED + DEFERRED + EVALUATED-REJECTED) in one SERP. Lets you compare inference signatures side-by-side within one query session.

| Trial | Query | Caption |
|---|---|---|
| [`p019-b4-t6`](../site/replay/trials/p019-b4-t6.html) | buy-panasonic-panasonic-kx-tpa50-cordless-handset | 1 CLK + 12 DEF + 1 REJ in one SERP — comparative inference |
| [`p037-b4-t1`](../site/replay/trials/p037-b4-t1.html) | buy-gates-gates-25474-radiator-coolant-hose | 1 CLK + 11 DEF + 2 REJ in one SERP — comparative inference |
| [`p014-b4-t7`](../site/replay/trials/p014-b4-t7.html) | buy-denso-denso-280-4260-remanufactured-starter | 2 CLK + 10 DEF + 1 REJ in one SERP — comparative inference |
| [`p006-b4-t7`](../site/replay/trials/p006-b4-t7.html) | buy-altaj-spanish-saffron-tin-1-ounce | 1 CLK + 9 DEF + 4 REJ in one SERP — comparative inference |
| [`p044-b1-t6`](../site/replay/trials/p044-b1-t6.html) | buy-bosch-bosch-68201-fuel-pump-gasket | 1 CLK + 9 DEF + 2 REJ in one SERP — comparative inference |
| [`p047-b6-t1`](../site/replay/trials/p047-b6-t1.html) | buy-avon-avon-glow-bronzing-pearls-bronzed | 1 CLK + 9 DEF + 1 REJ in one SERP — comparative inference |

### Group B-DEF — Canonical DEFERRED (6 trials)

Cursor enters a result, retreats, then returns. The retreat-and-return is the inference fingerprint that distinguishes 'still considering' from 'already rejected'.

| Trial | Query | Caption |
|---|---|---|
| [`p015-b5-t2`](../site/replay/trials/p015-b5-t2.html) | buy-clarins-the-it-kit | 11 DEFERRED AOIs — cursor entered, left, returned |
| [`p019-b1-t8`](../site/replay/trials/p019-b1-t8.html) | buy-vosene-vosene-original-medicated-shampoo-250ml | 11 DEFERRED AOIs — cursor entered, left, returned |
| [`p037-b1-t6`](../site/replay/trials/p037-b1-t6.html) | buy-halti-halti-headcollar-black-size-0 | 10 DEFERRED AOIs — cursor entered, left, returned |
| [`p011-b3-t1`](../site/replay/trials/p011-b3-t1.html) | buy-toshiba-pa5034u-1aca-ac-adapter | 9 DEFERRED AOIs — cursor entered, left, returned |
| [`p009-b1-t1`](../site/replay/trials/p009-b1-t1.html) | buy-new-chapter-prostate-5lx-120-softgels | 9 DEFERRED AOIs — cursor entered, left, returned |
| [`p028-b1-t2`](../site/replay/trials/p028-b1-t2.html) | buy-nars-nars-foreplay-orgasm-cheek-palette | 9 DEFERRED AOIs — cursor entered, left, returned |

### Group B-REJ — Canonical EVALUATED-REJECTED (0 trials)

Cursor enters a result, retreats, and never comes back during the trial. Multiple such AOIs in one trial show the rejection pattern at scale.

_No trials qualified at the current selection thresholds._

### Group C — Boundary cases (5 trials)

AOIs with dwell duration close to the 100ms classification threshold. Small changes in the threshold flip the label — the testbed cases that motivate threshold-sensitivity analysis.

| Trial | Query | Caption |
|---|---|---|
| [`p011-b6-t2`](../site/replay/trials/p011-b6-t2.html) | buy-delphi-delphi-ss10811-crankshaft-sensor | 5 AOI(s) with dwell near the 100ms classification threshold |
| [`p020-b6-t3`](../site/replay/trials/p020-b6-t3.html) | buy-luk-luk-16-113-clutch-set | 2 AOI(s) with dwell near the 100ms classification threshold |
| [`p042-b4-t5`](../site/replay/trials/p042-b4-t5.html) | buy-acdelco-acdelco-15036008-windshield-washer-motor | 2 AOI(s) with dwell near the 100ms classification threshold |
| [`p027-b3-t2`](../site/replay/trials/p027-b3-t2.html) | buy-bosch-bosch-97182-new-water-pump | 2 AOI(s) with dwell near the 100ms classification threshold |
| [`p035-b1-t2`](../site/replay/trials/p035-b1-t2.html) | buy-siskiyou-mlb-baltimore-orioles-toothbrush | 1 AOI(s) with dwell near the 100ms classification threshold |

### Group D — Cursor-vs-gaze divergence (5 trials)

AOIs heavily fixated by gaze (≥200ms) where the cursor never entered. A cursor-only classifier marks these NOT_APPROACHED, but the gaze evidence says they were considered. Diagnostic for cursor-as-attention-proxy assumptions.

| Trial | Query | Caption |
|---|---|---|
| [`p045-b2-t5`](../site/replay/trials/p045-b2-t5.html) | buy-rahua-rahua-jet-setter-kit | 5 AOI(s) gaze-fixated ≥200ms but cursor never entered — cursor-only would miss them |
| [`p045-b1-t10`](../site/replay/trials/p045-b1-t10.html) | buy-filorga-filorga-time-filler-50ml | 5 AOI(s) gaze-fixated ≥200ms but cursor never entered — cursor-only would miss them |
| [`p045-b3-t4`](../site/replay/trials/p045-b3-t4.html) | buy-swanson-strontium-citrate-2-bottles | 3 AOI(s) gaze-fixated ≥200ms but cursor never entered — cursor-only would miss them |
| [`p008-b1-t1`](../site/replay/trials/p008-b1-t1.html) | buy-mac-pro-mac-pigment-frozen-white | 3 AOI(s) gaze-fixated ≥200ms but cursor never entered — cursor-only would miss them |
| [`p028-b1-t9`](../site/replay/trials/p028-b1-t9.html) | buy-solaray-solaray-butterbur-60-capsules | 3 AOI(s) gaze-fixated ≥200ms but cursor never entered — cursor-only would miss them |

### Group A — Trivial calls (1 trials)

Cursor walks to one result and clicks. The label assignment is unambiguous — useful as a sanity check that the inference logic agrees on easy cases.

| Trial | Query | Caption |
|---|---|---|
| [`p008-b3-t7`](../site/replay/trials/p008-b3-t7.html) | buy-schildkraut-light-up-mirror-magnifier | Drive-through: 1 CLICKED + 4 not approached, 36.0s |

## Group F — Failure modes (manual)

Trials where the inference probably gets it wrong. Populated manually after reviewing groups B-DEF, B-REJ, C, D and noticing labels that disagree with the visible cursor pattern. None auto-assigned in this pass.

## Extended candidate pool

All 80 candidate trials are visible at `/replay/index.html`. The 23 group-assigned trials are highlighted there. The rest constitute an unstructured browseable set for spot-checking.
